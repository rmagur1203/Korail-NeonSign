let Base,SixDigitDate;_2ed‍.x([["GetMetroTimeTable_Flags",()=>GetMetroTimeTable_Flags],["GetMetroTimeTable",()=>GetMetroTimeTable],["Hour",()=>Hour],["Minute",()=>Minute]]);_2ed‍.w("./Base.js",[["default",["Base"],function(v){Base=v}]]);_2ed‍.w("./SixDigitDate.js",[["default",["SixDigitDate"],function(v){SixDigitDate=v}]]);


const Version = 20200801;
       const GetMetroTimeTable_Flags = Object.freeze({
    방향_상행: 1,
    방향_하행: 2,
    열차종별_고속: 64,
    열차종별_여객: 128,
    열차종별_화물: 256,
    열차종별_기타: 512,
    옵션_당역출발: 16,
    옵션_당역종착: 32,
    경유역_경유역출발: 16,
    경유역_경유역종착: 32,
    경유역_통과: 2048
});

const Flags = GetMetroTimeTable_Flags;

       class GetMetroTimeTable extends Base {
    constructor(station,
        page = 0,
        hr = Hour(new Date()),
        min = Minute(new Date()),
        directionFlag = Flags.방향_상행 | Flags.방향_하행,
        typeFlag = Flags.열차종별_고속 | Flags.열차종별_여객 | Flags.열차종별_화물 | Flags.열차종별_기타,
        viaStation = "",
        viaOption = Flags.경유역_경유역출발 | Flags.경유역_경유역종착) {
        super();
        this.request(`https://rail.blue/railroad/logis/getmetrotimetable.aspx?`
            + `pageIndex=${page}`
            + `&date=${SixDigitDate(date)}`
            + `&hr=${hr}`
            + `&min=${min}`
            + `&updn=${directionFlag}`
            + `&station=${encodeURI(station)}`
            + `&type=${typeFlag}`
            + `&via=${viaStation}`
            + `&viaopt=${viaOption}`
            + `&version=${Version}`);
    }

    get Flags() {
        
    }

    async GetResult() {
        return await this.Task;
    }
}

       function Hour(date) {
    return date.getHours();
}
       function Minute(date) {
    return Math.floor(date.getMinutes() / 10) * 10;
}